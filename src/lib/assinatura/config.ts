/**
 * Preço único da assinatura — fonte única usada pelo backend (cobrança PIX) e
 * pelo frontend (textos da tela de bloqueio/checkout). Mudar o preço é mudar
 * só aqui.
 */
export const VALOR_ASSINATURA = 149.9;
export const VALOR_ASSINATURA_FORMATADO = 'R$ 149,90';
